#! /usr/bin/env ruby

lib_path = File.expand_path(File.dirname(__FILE__)) + "/../lib/"
$LOAD_PATH << lib_path unless $LOAD_PATH.include? lib_path

require 'sudocoup'

host    = ARGV.shift
port    = ARGV.shift
ws_port = ARGV.shift

fork do
  Sudocoup::WebServer.run!
end
Sudocoup::Server.start(:host => host, :port => port, :ws_port => ws_port)
