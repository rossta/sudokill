require "rubygems"
require "rake"

namespace :sudokoup do
  
  namespace :server do
    
    task :echo do
      system "ruby examples/echo.rb"
    end
    
  end
end

require 'jasmine'
load 'jasmine/tasks/jasmine.rake'
